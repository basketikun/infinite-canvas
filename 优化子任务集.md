# BloomReel Optimization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Infinite Canvas 优化为类型可验证、安全边界清晰、画布核心可拆分、存储可扩展、工作台逻辑可复用的项目。

**Architecture:** 先建立质量保护网，再处理安全底座，然后按领域拆分 API、工作台、画布核心和持久化。每个任务必须能独立验证，避免把格式化、重构和功能变化混在同一次提交。

**Tech Stack:** Next.js 16、React 19、TypeScript、Zustand、localforage、Vitest、Ant Design、Express、Zod、Bun。

---

## 执行规则

- 执行前先检查 `git status --short`，确认并保护用户已有改动。
- 每个任务一个小 PR 或一个小提交。
- 触及 `web` 时优先运行 `bun run typecheck` 和 `bun run test:run`。
- 触及 `canvas-agent` 时先在 `canvas-agent` 目录执行 `bun install --frozen-lockfile`，再执行 `bun run build`。
- 触及 `docs` 时先在 `docs` 目录执行 `bun install --frozen-lockfile`，再执行 `bun run types:check`。
- 不在同一个提交里混合全仓格式化和业务改动。

## 文件结构目标

新增或重点修改的文件：

- `web/package.json`：新增 `typecheck`、`test`、`test:run` 脚本。
- `web/vitest.config.ts`：Web 单元测试配置。
- `web/src/test/setup.ts`：测试环境初始化。
- `web/src/services/api/api-error.ts`：统一 axios/fetch 错误解析。
- `web/src/services/api/request.ts`：扩展共享请求基础能力。
- `web/src/services/api/image-constraints.ts`：图片尺寸、比例、限制常量。
- `web/src/services/workbench/log-store.ts`：图片/视频工作台共享日志仓储。
- `web/src/services/security/webdav-target.ts`：WebDAV proxy 目标校验。
- `web/src/app/webdav-proxy/route.ts`：接入 SSRF 防护和日志脱敏。
- `web/src/stores/use-config-store.ts`：配置拆分、模型解析修复、敏感字段处理。
- `web/src/app/(user)/canvas/hooks/use-canvas-history.ts`：画布历史栈。
- `web/src/app/(user)/canvas/hooks/use-canvas-generation.ts`：画布生成执行。
- `web/src/app/(user)/canvas/hooks/use-canvas-project-state.ts`：当前项目装载和保存。
- `web/src/app/(user)/canvas/hooks/use-canvas-selection.ts`：选择框和多选。
- `web/src/app/(user)/canvas/utils/canvas-generation-config.ts`：生成配置纯函数。
- `web/src/app/(user)/canvas/utils/canvas-visible-graph.ts`：可见节点和可见连线计算。
- `web/src/app/(user)/canvas/storage/canvas-project-storage.ts`：项目分片存储。
- `canvas-agent/src/security.ts`：token 摘要、附件限制、请求大小策略。
- `canvas-agent/src/http-server.ts`：接入 Agent 加固。
- `.github/workflows/quality.yml`：质量门禁 CI。
- `README.md`、`SECURITY.md`、`docs/content/docs/...`：同步文档。

## 里程碑 0：基线保护

### Task 0.1：记录当前工作区状态

**Files:**
- Read: `D:\BloomReel\infinite-canvas`

- [ ] **Step 1: 查看未提交改动**

Run:

```powershell
cd D:\BloomReel\infinite-canvas
git status --short
```

Expected：能看到当前未提交文件列表，不修改任何文件。

- [ ] **Step 2: 查看关键未提交 diff**

Run:

```powershell
cd D:\BloomReel\infinite-canvas
git diff -- web/package.json web/src/app/(user)/page.tsx web/src/components/prompts/prompt-card.tsx web/src/components/prompts/prompt-detail-dialog.tsx
```

Expected：确认这些改动是否与当前任务重叠。若重叠，后续编辑只追加必要改动，不回滚用户变更。

### Task 0.2：新增 Web 质量脚本

**Files:**
- Modify: `web/package.json`

- [ ] **Step 1: 修改 scripts**

将 `web/package.json` 的 `scripts` 调整为包含以下键：

```json
{
  "dev": "next dev --webpack -H 0.0.0.0 -p 3025",
  "build": "next build",
  "start": "next start",
  "typecheck": "tsc --noEmit",
  "test": "vitest",
  "test:run": "vitest run",
  "format": "prettier --write .",
  "format:check": "prettier --check ."
}
```

- [ ] **Step 2: 添加测试依赖**

在 `web/package.json` 的 `devDependencies` 中添加：

```json
{
  "@vitejs/plugin-react": "^5.1.0",
  "jsdom": "^27.3.0",
  "vitest": "^4.0.16"
}
```

- [ ] **Step 3: 安装依赖**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun install
```

Expected：`web/bun.lock` 更新，`node_modules` 中包含 `vitest`。

- [ ] **Step 4: 验证脚本可运行**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run typecheck
```

Expected：命令执行到 TypeScript 错误列表，错误内容与基线相同或更少。

### Task 0.3：新增 Vitest 配置

**Files:**
- Create: `web/vitest.config.ts`
- Create: `web/src/test/setup.ts`

- [ ] **Step 1: 创建 `web/vitest.config.ts`**

```ts
import path from "node:path";
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";

export default defineConfig({
    plugins: [react()],
    test: {
        environment: "jsdom",
        globals: true,
        setupFiles: ["./src/test/setup.ts"],
        include: ["src/**/*.test.ts", "src/**/*.test.tsx"],
    },
    resolve: {
        alias: {
            "@": path.resolve(__dirname, "src"),
        },
    },
});
```

- [ ] **Step 2: 创建 `web/src/test/setup.ts`**

```ts
import { afterEach, vi } from "vitest";

afterEach(() => {
    vi.restoreAllMocks();
});
```

- [ ] **Step 3: 运行空测试集**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run test:run
```

Expected：Vitest 启动成功，若没有测试文件则显示没有匹配测试或 0 tests。若 Vitest 默认因无测试退出非零，下一步先添加第一个测试再验证。

## 里程碑 1：修复类型基线

### Task 1.1：修复已知 TypeScript 错误

**Files:**
- Modify: `web/src/app/(user)/canvas/components/canvas-assistant-panel.tsx`
- Modify: `web/src/app/(user)/canvas/components/canvas-image-toolbar-settings-modal.tsx`
- Modify: `web/src/app/(user)/canvas/components/canvas-node-hover-toolbar.tsx`
- Modify: `web/src/app/(user)/canvas/components/canvas-node-mask-edit-dialog.tsx`
- Modify: `web/src/app/(user)/canvas/components/asset-picker-modal.tsx`
- Modify: `web/src/app/(user)/image/page.tsx`
- Modify: `web/src/app/(user)/video/page.tsx`
- Modify: `web/src/app/api/prompts/route.ts`
- Modify: `web/src/services/api/image.ts`
- Modify: `web/src/stores/use-config-store.ts`

- [ ] **Step 1: 修复 Promise 条件判断**

在 `canvas-assistant-panel.tsx` 第 810 行附近，找到对 `Promise<boolean>` 的同步条件判断。将逻辑改为 `await` 后判断，或把函数改成返回同步 boolean。修改后该分支不再出现 TS2801。

- [ ] **Step 2: 修复 tool role 类型**

在 `canvas-assistant-panel.tsx` 第 1271 行附近，将只接受 `"tool"` 的对象构造收窄到 tool 消息。示例形态：

```ts
if (message.role === "tool") {
    return {
        ...message,
        role: "tool" as const,
    };
}
```

- [ ] **Step 3: 修复 nullable Element**

在 `canvas-image-toolbar-settings-modal.tsx` 第 105 行附近，调用需要 `Element` 的 API 前先判断 ref：

```ts
if (!containerRef.current) return;
someDomApi(containerRef.current);
```

- [ ] **Step 4: 修复 hover toolbar nullable node**

在 `canvas-node-hover-toolbar.tsx` 第 129 行附近，读取 `node` 字段前增加早返回：

```ts
if (!node) return null;
```

- [ ] **Step 5: 修复 Ant Design Tooltip styles 类型**

在 `canvas-node-hover-toolbar.tsx` 第 285 行附近，把不存在的 `body` 样式键改为 Ant Design 6 支持的键。优先使用：

```ts
styles={{ root: { pointerEvents: "none" } }}
```

具体属性以当前组件实际传入的 Tooltip API 为准，目标是去掉 TS2353。

- [ ] **Step 6: 修复 mask dialog canvas nullable**

在 `canvas-node-mask-edit-dialog.tsx` 第 60 行附近，调用绘制函数前判断 canvas：

```ts
if (!canvas) return;
drawMask(canvas);
```

- [ ] **Step 7: 统一 AssetPickerModal props**

当前 `image/page.tsx` 和 `video/page.tsx` 传入了 `defaultTab`，但 `asset-picker-modal.tsx` 的 Props 没有该字段。选择一种方案：

方案 A：在 `asset-picker-modal.tsx` 添加 `defaultTab?: "text" | "image" | "video"` 并接入初始 tab。
方案 B：移除调用方的 `defaultTab`。

推荐方案 A，因为调用方已经表达了产品意图。

- [ ] **Step 8: 修复正则目标版本错误**

`web/tsconfig.json` 当前 target 为 `ES2017`，`app/api/prompts/route.ts` 使用了 ES2018 才支持的正则 flag。优先将 target 改为：

```json
{
  "compilerOptions": {
    "target": "ES2018"
  }
}
```

改动后运行类型检查确认 TS1501 消失。

- [ ] **Step 9: 修复 `match.index` undefined**

在 `web/src/services/api/image.ts` 的 SSE 解析处，将 `match.index` 读取改为带默认值：

```ts
const matchIndex = match.index ?? 0;
```

- [ ] **Step 10: 修复 `decodeChannelModel` 后的 never 类型**

在 `use-config-store.ts` 的 `normalizeModelOptionValue` 中，`decoded` 在非 decoded 分支已经是 null，不要继续使用 `decoded?.model`。改为：

```ts
const rawModel = decoded?.model || model;
const channel = decoded ? channels.find((item) => item.id === decoded.channelId) : channels.find((item) => item.models.includes(rawModel)) || channels[0];
return channel && channel.models.includes(rawModel) ? encodeChannelModel(channel.id, rawModel) : model;
```

- [ ] **Step 11: 验证类型清零**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run typecheck
```

Expected：0 TypeScript errors。

### Task 1.2：关闭构建类型错误忽略

**Files:**
- Modify: `web/next.config.ts`

- [ ] **Step 1: 删除 ignoreBuildErrors**

将：

```ts
typescript: {
    ignoreBuildErrors: true,
},
```

删除，或改成不忽略类型错误的默认配置。

- [ ] **Step 2: 运行构建**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run build
```

Expected：Next build 不再因为 TypeScript 错误失败。

## 里程碑 2：核心测试保护网

### Task 2.1：为模型渠道解析补测试

**Files:**
- Create: `web/src/stores/use-config-store.test.ts`
- Modify: `web/src/stores/use-config-store.ts`

- [ ] **Step 1: 编写测试**

```ts
import { describe, expect, it } from "vitest";
import { createModelChannel, decodeChannelModel, encodeChannelModel, modelOptionName, normalizeModelOptionValue, resolveModelRequestConfig, type AiConfig, defaultConfig } from "./use-config-store";

describe("model channel helpers", () => {
    it("encodes and decodes channel model values", () => {
        const value = encodeChannelModel("default", "gpt-image-2");
        expect(value).toBe("default::gpt-image-2");
        expect(decodeChannelModel(value)).toEqual({ channelId: "default", model: "gpt-image-2" });
        expect(modelOptionName(value)).toBe("gpt-image-2");
    });

    it("normalizes raw model names to the first matching channel", () => {
        const channels = [
            createModelChannel({ id: "a", models: ["gpt-image-2"] }),
            createModelChannel({ id: "b", models: ["gpt-5.5"] }),
        ];
        expect(normalizeModelOptionValue("gpt-5.5", channels)).toBe("b::gpt-5.5");
    });

    it("resolves request config from the selected channel", () => {
        const config: AiConfig = {
            ...defaultConfig,
            channels: [createModelChannel({ id: "remote", baseUrl: "https://example.com", apiKey: "key", models: ["gpt-5.5"] })],
        };
        const resolved = resolveModelRequestConfig(config, "remote::gpt-5.5");
        expect(resolved.model).toBe("gpt-5.5");
        expect(resolved.baseUrl).toBe("https://example.com");
        expect(resolved.apiKey).toBe("key");
    });
});
```

- [ ] **Step 2: 导出测试所需函数**

如果 `normalizeModelOptionValue` 尚未导出，保持当前导出状态或显式导出。不得为测试复制实现。

- [ ] **Step 3: 运行测试**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run test:run src/stores/use-config-store.test.ts
```

Expected：3 tests passed。

### Task 2.2：为 Canvas Agent ops 补测试

**Files:**
- Create: `web/src/app/(user)/canvas/utils/canvas-agent-ops.test.ts`

- [ ] **Step 1: 编写测试**

```ts
import { describe, expect, it } from "vitest";
import { CanvasNodeType } from "../types";
import { applyCanvasAgentOps, type CanvasAgentSnapshot } from "./canvas-agent-ops";

const snapshot: CanvasAgentSnapshot = {
    projectId: "p1",
    title: "Test",
    nodes: [],
    connections: [],
    selectedNodeIds: [],
    viewport: { x: 0, y: 0, k: 1 },
};

describe("applyCanvasAgentOps", () => {
    it("adds a node and selects it", () => {
        const result = applyCanvasAgentOps(snapshot, [{ type: "add_node", id: "n1", nodeType: CanvasNodeType.Text, title: "Note", x: 10, y: 20 }]);
        expect(result.nodes).toHaveLength(1);
        expect(result.nodes[0].position).toEqual({ x: 10, y: 20 });
        expect(result.selectedNodeIds).toEqual(["n1"]);
    });

    it("connects existing nodes only once", () => {
        const withNodes = applyCanvasAgentOps(snapshot, [
            { type: "add_node", id: "a", nodeType: CanvasNodeType.Text },
            { type: "add_node", id: "b", nodeType: CanvasNodeType.Text },
        ]);
        const result = applyCanvasAgentOps(withNodes, [
            { type: "connect_nodes", fromNodeId: "a", toNodeId: "b" },
            { type: "connect_nodes", fromNodeId: "a", toNodeId: "b" },
        ]);
        expect(result.connections).toHaveLength(1);
    });

    it("deletes related connections when deleting a node", () => {
        const withGraph = applyCanvasAgentOps(snapshot, [
            { type: "add_node", id: "a", nodeType: CanvasNodeType.Text },
            { type: "add_node", id: "b", nodeType: CanvasNodeType.Text },
            { type: "connect_nodes", fromNodeId: "a", toNodeId: "b" },
        ]);
        const result = applyCanvasAgentOps(withGraph, [{ type: "delete_node", id: "a" }]);
        expect(result.nodes.map((node) => node.id)).toEqual(["b"]);
        expect(result.connections).toEqual([]);
    });
});
```

- [ ] **Step 2: 运行测试**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run test:run src/app/(user)/canvas/utils/canvas-agent-ops.test.ts
```

Expected：3 tests passed。

### Task 2.3：为资源引用规则补测试

**Files:**
- Create: `web/src/app/(user)/canvas/utils/canvas-resource-references.test.ts`

- [ ] **Step 1: 编写测试**

```ts
import { describe, expect, it } from "vitest";
import { CanvasNodeType, type CanvasConnection, type CanvasNodeData } from "../types";
import { buildNodeMentionReferences, getGenerationResourceNodes } from "./canvas-resource-references";

const nodes: CanvasNodeData[] = [
    { id: "image-1", type: CanvasNodeType.Image, title: "Image", position: { x: 0, y: 0 }, width: 100, height: 100, metadata: { content: "blob:image" } },
    { id: "text-1", type: CanvasNodeType.Text, title: "Prompt", position: { x: 0, y: 0 }, width: 100, height: 100, metadata: { content: "hello" } },
    { id: "config-1", type: CanvasNodeType.Config, title: "Config", position: { x: 0, y: 0 }, width: 100, height: 100, metadata: {} },
];

const connections: CanvasConnection[] = [
    { id: "c1", fromNodeId: "image-1", toNodeId: "config-1" },
    { id: "c2", fromNodeId: "text-1", toNodeId: "config-1" },
];

describe("canvas resource references", () => {
    it("uses connected resources for generation", () => {
        expect(getGenerationResourceNodes("config-1", nodes, connections).map((node) => node.id)).toEqual(["image-1", "text-1"]);
    });

    it("labels mention references by kind", () => {
        const references = buildNodeMentionReferences(nodes[2], nodes, connections);
        expect(references.map((item) => item.kind)).toEqual(["image", "text"]);
        expect(references.every((item) => item.active)).toBe(true);
    });
});
```

- [ ] **Step 2: 运行测试**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run test:run src/app/(user)/canvas/utils/canvas-resource-references.test.ts
```

Expected：2 tests passed。

## 里程碑 3：WebDAV proxy 安全

### Task 3.1：新增 WebDAV 目标校验模块

**Files:**
- Create: `web/src/services/security/webdav-target.ts`
- Create: `web/src/services/security/webdav-target.test.ts`

- [ ] **Step 1: 创建校验模块**

```ts
const BLOCKED_HOSTS = new Set(["localhost", "metadata.google.internal"]);
const BLOCKED_IPV4_EXACT = new Set(["0.0.0.0", "127.0.0.1", "169.254.169.254"]);

export function validateWebdavTarget(target: string) {
    let url: URL;
    try {
        url = new URL(target);
    } catch {
        return { ok: false as const, reason: "Invalid x-webdav-target" };
    }

    if (url.protocol !== "http:" && url.protocol !== "https:") {
        return { ok: false as const, reason: "Unsupported WebDAV target" };
    }

    const hostname = url.hostname.toLowerCase();
    if (BLOCKED_HOSTS.has(hostname) || hostname.endsWith(".localhost")) {
        return { ok: false as const, reason: "Blocked WebDAV target host" };
    }

    if (isBlockedIpv4(hostname) || isBlockedIpv6(hostname)) {
        return { ok: false as const, reason: "Blocked WebDAV target address" };
    }

    return { ok: true as const, url };
}

export function sanitizeWebdavTargetForLog(url: URL) {
    return `${url.protocol}//${url.host}${url.pathname}`;
}

function isBlockedIpv4(hostname: string) {
    if (BLOCKED_IPV4_EXACT.has(hostname)) return true;
    const parts = hostname.split(".").map((part) => Number(part));
    if (parts.length !== 4 || parts.some((part) => !Number.isInteger(part) || part < 0 || part > 255)) return false;
    const [a, b] = parts;
    return a === 10 || a === 127 || (a === 172 && b >= 16 && b <= 31) || (a === 192 && b === 168) || (a === 169 && b === 254) || (a === 100 && b >= 64 && b <= 127);
}

function isBlockedIpv6(hostname: string) {
    const value = hostname.replace(/^\[|\]$/g, "").toLowerCase();
    return value === "::1" || value.startsWith("fc") || value.startsWith("fd") || value.startsWith("fe80:");
}
```

- [ ] **Step 2: 创建测试**

```ts
import { describe, expect, it } from "vitest";
import { sanitizeWebdavTargetForLog, validateWebdavTarget } from "./webdav-target";

describe("validateWebdavTarget", () => {
    it("allows public https targets", () => {
        expect(validateWebdavTarget("https://dav.example.com/remote.php/dav/files/a?token=secret").ok).toBe(true);
    });

    it("blocks localhost and private ipv4 targets", () => {
        expect(validateWebdavTarget("http://localhost:8080").ok).toBe(false);
        expect(validateWebdavTarget("http://127.0.0.1:8080").ok).toBe(false);
        expect(validateWebdavTarget("http://10.0.0.5").ok).toBe(false);
        expect(validateWebdavTarget("http://172.16.0.5").ok).toBe(false);
        expect(validateWebdavTarget("http://192.168.1.2").ok).toBe(false);
        expect(validateWebdavTarget("http://169.254.169.254").ok).toBe(false);
    });

    it("removes query string from logs", () => {
        const result = validateWebdavTarget("https://dav.example.com/path?password=secret");
        expect(result.ok && sanitizeWebdavTargetForLog(result.url)).toBe("https://dav.example.com/path");
    });
});
```

- [ ] **Step 3: 运行测试**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run test:run src/services/security/webdav-target.test.ts
```

Expected：3 tests passed。

### Task 3.2：接入 proxy route

**Files:**
- Modify: `web/src/app/webdav-proxy/route.ts`

- [ ] **Step 1: 替换 URL 解析逻辑**

将 route 中的 `new URL(target)` 和协议判断替换为：

```ts
const validation = validateWebdavTarget(target);
if (!validation.ok) return new Response(validation.reason, { status: 400 });
const url = validation.url;
const safeTarget = sanitizeWebdavTargetForLog(url);
```

- [ ] **Step 2: 限制方法**

在读取 method 后增加：

```ts
const allowedMethods = new Set(["GET", "HEAD", "PUT", "PROPFIND", "MKCOL"]);
if (!allowedMethods.has(method)) return new Response("Unsupported WebDAV method", { status: 405 });
```

- [ ] **Step 3: 日志脱敏**

将日志中的 `url.href` 改为 `safeTarget`：

```ts
console.log(`[webdav-proxy] ${method} ${safeTarget} ${body?.byteLength || 0}B`);
console.log(`[webdav-proxy] ${method} ${safeTarget} -> ${response.status}`);
```

- [ ] **Step 4: 验证**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run typecheck
bun run test:run src/services/security/webdav-target.test.ts
```

Expected：typecheck 通过，测试通过。

## 里程碑 4：API 请求基础设施

### Task 4.1：抽统一错误处理

**Files:**
- Create: `web/src/services/api/api-error.ts`
- Create: `web/src/services/api/api-error.test.ts`
- Modify: `web/src/services/api/image.ts`
- Modify: `web/src/services/api/audio.ts`
- Modify: `web/src/services/api/video.ts`

- [ ] **Step 1: 创建错误处理模块**

```ts
import axios from "axios";

type ApiErrorBody = { error?: { message?: string }; msg?: string; message?: string; code?: number | string };

export function readAxiosApiError(error: unknown, fallback: string) {
    if (axios.isCancel(error)) return "请求已取消";
    if (axios.isAxiosError<ApiErrorBody>(error)) {
        const data = error.response?.data;
        const detail = data?.error?.message || data?.msg || data?.message || error.message;
        return detail ? `${fallback}：${detail}` : fallbackWithStatus(fallback, error.response?.status);
    }
    if (error instanceof DOMException && error.name === "AbortError") return "请求已取消";
    return error instanceof Error ? error.message : fallback;
}

export async function readFetchApiError(response: Response, fallback: string) {
    const detail = await response.text().catch(() => "");
    if (!detail) return fallbackWithStatus(fallback, response.status);
    try {
        const data = JSON.parse(detail) as ApiErrorBody;
        return data.error?.message || data.msg || data.message || `${fallback}：${response.status}`;
    } catch {
        return `${fallback}：${response.status} ${detail.slice(0, 200)}`;
    }
}

function fallbackWithStatus(fallback: string, status?: number) {
    return status ? `${fallback}：${status}` : fallback;
}
```

- [ ] **Step 2: 创建测试**

```ts
import { describe, expect, it } from "vitest";
import { readFetchApiError } from "./api-error";

describe("readFetchApiError", () => {
    it("reads json error messages", async () => {
        const response = new Response(JSON.stringify({ error: { message: "bad request" } }), { status: 400 });
        await expect(readFetchApiError(response, "请求失败")).resolves.toBe("bad request");
    });

    it("falls back to status and text", async () => {
        const response = new Response("plain error", { status: 502 });
        await expect(readFetchApiError(response, "请求失败")).resolves.toBe("请求失败：502 plain error");
    });
});
```

- [ ] **Step 3: 替换重复实现**

在 `image.ts`、`audio.ts`、`video.ts` 中删除各自的 `readAxiosError`，改用 `readAxiosApiError`。fetch 错误读取改用 `readFetchApiError`。

- [ ] **Step 4: 验证**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run test:run src/services/api/api-error.test.ts
bun run typecheck
```

Expected：测试通过，typecheck 通过。

### Task 4.2：抽图片约束常量

**Files:**
- Create: `web/src/services/api/image-constraints.ts`
- Create: `web/src/services/api/image-constraints.test.ts`
- Modify: `web/src/services/api/image.ts`

- [ ] **Step 1: 提取常量和解析函数**

将 `image.ts` 中图片大小、比例、模型限制常量移动到 `image-constraints.ts`。至少导出：

```ts
export const IMAGE_SIZE_OPTIONS = ["auto", "1:1", "4:3", "3:4", "16:9", "9:16"] as const;
export const MAX_IMAGE_RATIO = 3;

export function parseImageRatio(size: string) {
    if (!size || size === "auto") return null;
    const [rawWidth, rawHeight] = size.split(":").map(Number);
    if (!Number.isFinite(rawWidth) || !Number.isFinite(rawHeight) || rawWidth <= 0 || rawHeight <= 0) return null;
    const ratio = rawWidth / rawHeight;
    if (ratio <= 0 || ratio > MAX_IMAGE_RATIO || 1 / ratio > MAX_IMAGE_RATIO) return null;
    return ratio;
}
```

- [ ] **Step 2: 添加测试**

```ts
import { describe, expect, it } from "vitest";
import { parseImageRatio } from "./image-constraints";

describe("parseImageRatio", () => {
    it("accepts valid ratios", () => {
        expect(parseImageRatio("1:1")).toBe(1);
        expect(parseImageRatio("16:9")).toBeCloseTo(16 / 9);
    });

    it("rejects invalid ratios", () => {
        expect(parseImageRatio("auto")).toBeNull();
        expect(parseImageRatio("abc")).toBeNull();
        expect(parseImageRatio("99:1")).toBeNull();
        expect(parseImageRatio("1:0")).toBeNull();
    });
});
```

- [ ] **Step 3: 验证**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run test:run src/services/api/image-constraints.test.ts
bun run typecheck
```

Expected：测试通过，typecheck 通过。

## 里程碑 5：敏感配置治理

### Task 5.1：增加敏感配置清理能力

**Files:**
- Modify: `web/src/stores/use-config-store.ts`
- Modify: `web/src/components/layout/app-config-modal.tsx`

- [ ] **Step 1: 扩展 ConfigStore action**

在 `ConfigStore` 类型中增加：

```ts
clearSecrets: () => void;
```

在 store 实现中增加：

```ts
clearSecrets: () =>
    set((state) => ({
        config: {
            ...state.config,
            apiKey: "",
            channels: state.config.channels.map((channel) => ({ ...channel, apiKey: "" })),
        },
        webdav: {
            ...state.webdav,
            password: "",
        },
    })),
```

- [ ] **Step 2: 配置弹窗增加清理按钮**

在 `app-config-modal.tsx` 的配置弹窗底部增加“清理敏感信息”按钮。点击后调用 `clearSecrets`，并提示：

```ts
message.success("已清理 API Key 和 WebDAV 密码");
```

- [ ] **Step 3: 验证**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run typecheck
```

Manual Expected：打开配置弹窗，填写 API Key 和 WebDAV 密码，点击清理后字段变空。

### Task 5.2：规划 secrets 分离存储

**Files:**
- Create: `web/src/stores/use-secret-store.ts`
- Modify: `web/src/stores/use-config-store.ts`
- Modify: `web/src/components/layout/app-config-modal.tsx`

- [ ] **Step 1: 创建 secret store**

```ts
import { create } from "zustand";
import { persist } from "zustand/middleware";

type SecretStore = {
    apiKeysByChannelId: Record<string, string>;
    webdavPassword: string;
    agentToken: string;
    setChannelApiKey: (channelId: string, apiKey: string) => void;
    setWebdavPassword: (password: string) => void;
    setAgentToken: (token: string) => void;
    clearSecrets: () => void;
};

export const useSecretStore = create<SecretStore>()(
    persist(
        (set) => ({
            apiKeysByChannelId: {},
            webdavPassword: "",
            agentToken: "",
            setChannelApiKey: (channelId, apiKey) => set((state) => ({ apiKeysByChannelId: { ...state.apiKeysByChannelId, [channelId]: apiKey } })),
            setWebdavPassword: (webdavPassword) => set({ webdavPassword }),
            setAgentToken: (agentToken) => set({ agentToken }),
            clearSecrets: () => set({ apiKeysByChannelId: {}, webdavPassword: "", agentToken: "" }),
        }),
        { name: "infinite-canvas:secret_store" },
    ),
);
```

- [ ] **Step 2: 迁移读取路径**

在 `resolveModelRequestConfig` 前，保证最终请求配置能从 secret store 或旧 channel 字段读取 API Key。第一阶段保留旧字段兼容。

- [ ] **Step 3: 加迁移说明**

在 `SECURITY.md` 增加一节说明：旧版本会把敏感配置放在 `infinite-canvas:ai_config_store`，新版本提供清理入口。

## 里程碑 6：画布核心拆分

### Task 6.1：抽生成配置纯函数

**Files:**
- Create: `web/src/app/(user)/canvas/utils/canvas-generation-config.ts`
- Create: `web/src/app/(user)/canvas/utils/canvas-generation-config.test.ts`
- Modify: `web/src/app/(user)/canvas/[id]/canvas-client-page.tsx`

- [ ] **Step 1: 移动 `buildGenerationConfig`**

从 `canvas-client-page.tsx` 移出 `buildGenerationConfig`，放入新文件，并导出：

```ts
import { defaultConfig, type AiConfig } from "@/stores/use-config-store";
import type { CanvasNodeData, CanvasGenerationMode } from "../types";

export function buildCanvasGenerationConfig(config: AiConfig, node: CanvasNodeData | undefined, mode: CanvasGenerationMode): AiConfig {
    const defaultModel = mode === "image" ? config.imageModel : mode === "video" ? config.videoModel : mode === "audio" ? config.audioModel : config.textModel;
    return {
        ...config,
        model: node?.metadata?.model || defaultModel || (mode === "audio" ? defaultConfig.audioModel : config.model || defaultConfig.model),
        quality: node?.metadata?.quality || config.quality || defaultConfig.quality,
        size: node?.metadata?.size || config.size || defaultConfig.size,
        videoSeconds: node?.metadata?.seconds || config.videoSeconds || defaultConfig.videoSeconds,
        vquality: node?.metadata?.vquality || config.vquality || defaultConfig.vquality,
        videoGenerateAudio: node?.metadata?.generateAudio || config.videoGenerateAudio || defaultConfig.videoGenerateAudio,
        videoWatermark: node?.metadata?.watermark || config.videoWatermark || defaultConfig.videoWatermark,
        audioVoice: node?.metadata?.audioVoice || config.audioVoice || defaultConfig.audioVoice,
        audioFormat: node?.metadata?.audioFormat || config.audioFormat || defaultConfig.audioFormat,
        audioSpeed: node?.metadata?.audioSpeed || config.audioSpeed || defaultConfig.audioSpeed,
        audioInstructions: node?.metadata?.audioInstructions || config.audioInstructions || defaultConfig.audioInstructions,
        count: String(node?.metadata?.count || (mode === "image" ? config.canvasImageCount || config.count : config.count) || defaultConfig.count),
    };
}
```

- [ ] **Step 2: 添加测试**

```ts
import { describe, expect, it } from "vitest";
import { defaultConfig } from "@/stores/use-config-store";
import { CanvasNodeType, type CanvasNodeData } from "../types";
import { buildCanvasGenerationConfig } from "./canvas-generation-config";

describe("buildCanvasGenerationConfig", () => {
    it("uses capability default model", () => {
        const config = { ...defaultConfig, imageModel: "default::gpt-image-2", videoModel: "default::video-model" };
        expect(buildCanvasGenerationConfig(config, undefined, "video").model).toBe("default::video-model");
    });

    it("lets node metadata override model and size", () => {
        const node: CanvasNodeData = {
            id: "n1",
            type: CanvasNodeType.Config,
            title: "Config",
            position: { x: 0, y: 0 },
            width: 100,
            height: 100,
            metadata: { model: "custom::model", size: "16:9", count: 2 },
        };
        const result = buildCanvasGenerationConfig(defaultConfig, node, "image");
        expect(result.model).toBe("custom::model");
        expect(result.size).toBe("16:9");
        expect(result.count).toBe("2");
    });
});
```

- [ ] **Step 3: 替换页面引用**

将 `canvas-client-page.tsx` 中的 `buildGenerationConfig` 调用替换为 `buildCanvasGenerationConfig`。

- [ ] **Step 4: 验证**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run test:run src/app/(user)/canvas/utils/canvas-generation-config.test.ts
bun run typecheck
```

Expected：测试通过，typecheck 通过。

### Task 6.2：抽历史栈 hook

**Files:**
- Create: `web/src/app/(user)/canvas/hooks/use-canvas-history.ts`
- Modify: `web/src/app/(user)/canvas/[id]/canvas-client-page.tsx`

- [ ] **Step 1: 定义 hook 输入输出**

新 hook 负责：

- 保存历史栈。
- debounced commit。
- undo/redo。
- resetProjectHistory。

导出形态：

```ts
export type CanvasHistorySnapshot = {
    nodes: CanvasNodeData[];
    connections: CanvasConnection[];
    chatSessions: CanvasAssistantSession[];
};

export function useCanvasHistory(initial: CanvasHistorySnapshot) {
    return {
        historyState,
        commitHistory,
        undoCanvas,
        redoCanvas,
        resetHistory,
    };
}
```

- [ ] **Step 2: 先复制原逻辑，不改行为**

从页面中移动 history refs、timer 和 undo/redo 逻辑。移动过程中保持 debounce 时间 180ms 不变。

- [ ] **Step 3: 页面接入 hook**

页面中只保留：

```ts
const { historyState, commitHistory, undoCanvas, redoCanvas, resetHistory } = useCanvasHistory({ nodes, connections, chatSessions });
```

- [ ] **Step 4: 手动验证**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run typecheck
```

Manual Expected：打开画布，新建节点、移动节点、撤销、重做均正常。

### Task 6.3：抽可见图计算

**Files:**
- Create: `web/src/app/(user)/canvas/utils/canvas-visible-graph.ts`
- Create: `web/src/app/(user)/canvas/utils/canvas-visible-graph.test.ts`
- Modify: `web/src/app/(user)/canvas/[id]/canvas-client-page.tsx`

- [ ] **Step 1: 创建计算函数**

```ts
import type { CanvasConnection, CanvasNodeData, ViewportTransform } from "../types";

export function getVisibleNodes(nodes: CanvasNodeData[], viewport: ViewportTransform, viewportSize: { width: number; height: number }, padding = 400) {
    const left = (-viewport.x - padding) / viewport.k;
    const top = (-viewport.y - padding) / viewport.k;
    const right = (-viewport.x + viewportSize.width + padding) / viewport.k;
    const bottom = (-viewport.y + viewportSize.height + padding) / viewport.k;
    return nodes.filter((node) => node.position.x + node.width >= left && node.position.x <= right && node.position.y + node.height >= top && node.position.y <= bottom);
}

export function getVisibleConnections(connections: CanvasConnection[], visibleNodeIds: Set<string>) {
    return connections.filter((connection) => visibleNodeIds.has(connection.fromNodeId) || visibleNodeIds.has(connection.toNodeId));
}
```

- [ ] **Step 2: 添加测试**

```ts
import { describe, expect, it } from "vitest";
import { CanvasNodeType, type CanvasNodeData } from "../types";
import { getVisibleConnections, getVisibleNodes } from "./canvas-visible-graph";

const node = (id: string, x: number): CanvasNodeData => ({
    id,
    type: CanvasNodeType.Text,
    title: id,
    position: { x, y: 0 },
    width: 100,
    height: 100,
    metadata: {},
});

describe("canvas visible graph", () => {
    it("filters nodes by viewport bounds", () => {
        const result = getVisibleNodes([node("a", 0), node("b", 5000)], { x: 0, y: 0, k: 1 }, { width: 800, height: 600 }, 0);
        expect(result.map((item) => item.id)).toEqual(["a"]);
    });

    it("keeps connections touching visible nodes", () => {
        const result = getVisibleConnections(
            [
                { id: "c1", fromNodeId: "a", toNodeId: "b" },
                { id: "c2", fromNodeId: "x", toNodeId: "y" },
            ],
            new Set(["a"]),
        );
        expect(result.map((item) => item.id)).toEqual(["c1"]);
    });
});
```

- [ ] **Step 3: 页面接入**

页面中用 `getVisibleNodes` 替换内联 `visibleNodes` 计算，用 `getVisibleConnections` 替换全量 connections 过滤。

- [ ] **Step 4: 验证**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run test:run src/app/(user)/canvas/utils/canvas-visible-graph.test.ts
bun run typecheck
```

Manual Expected：打开已有画布，节点和连线显示正常，拖动画布时连线不丢失。

## 里程碑 7：项目存储分片

### Task 7.1：实现项目分片存储适配层

**Files:**
- Create: `web/src/app/(user)/canvas/storage/canvas-project-storage.ts`
- Create: `web/src/app/(user)/canvas/storage/canvas-project-storage.test.ts`
- Modify: `web/src/app/(user)/canvas/stores/use-canvas-store.ts`

- [ ] **Step 1: 定义存储 key**

```ts
export const CANVAS_PROJECT_INDEX_KEY = "infinite-canvas:canvas_project_index";
export const CANVAS_PROJECT_KEY_PREFIX = "infinite-canvas:canvas_project:";
export const LEGACY_CANVAS_STORE_KEY = "infinite-canvas:canvas_store";
```

- [ ] **Step 2: 实现接口**

```ts
import { localForageStorage } from "@/lib/localforage-storage";
import type { CanvasProject } from "../stores/use-canvas-store";

export type CanvasProjectIndexItem = Pick<CanvasProject, "id" | "title" | "createdAt" | "updatedAt">;

export async function saveCanvasProject(project: CanvasProject) {
    await localForageStorage.setItem(`${CANVAS_PROJECT_KEY_PREFIX}${project.id}`, JSON.stringify(project));
}

export async function loadCanvasProject(id: string) {
    const value = await localForageStorage.getItem(`${CANVAS_PROJECT_KEY_PREFIX}${id}`);
    return value ? (JSON.parse(value) as CanvasProject) : null;
}

export async function saveCanvasProjectIndex(projects: CanvasProjectIndexItem[]) {
    await localForageStorage.setItem(CANVAS_PROJECT_INDEX_KEY, JSON.stringify({ version: 1, projects }));
}

export async function loadCanvasProjectIndex() {
    const value = await localForageStorage.getItem(CANVAS_PROJECT_INDEX_KEY);
    if (!value) return [];
    const parsed = JSON.parse(value) as { projects?: CanvasProjectIndexItem[] };
    return Array.isArray(parsed.projects) ? parsed.projects : [];
}
```

- [ ] **Step 3: 添加迁移读取**

实现 `loadLegacyCanvasProjects`，读取旧的 `LEGACY_CANVAS_STORE_KEY` 并解析 `state.projects`。迁移时写入新 index 和每个 project，保留旧 key 不删除。

- [ ] **Step 4: 添加测试**

使用 mock storage 或 fake localforage storage 验证：

- 保存项目后能读取。
- 保存 index 后顺序不变。
- legacy store 能迁移出 projects。

- [ ] **Step 5: 接入 Zustand store**

先保持 store 对外 API 不变：`projects` 仍是数组。内部 rehydrate 改为从分片加载，`updateProject` 只保存被修改的项目和 index。

- [ ] **Step 6: 验证**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run test:run src/app/(user)/canvas/storage/canvas-project-storage.test.ts
bun run typecheck
```

Manual Expected：旧画布能打开，新建、重命名、删除项目正常。

### Task 7.2：WebDAV 同步兼容分片存储

**Files:**
- Modify: `web/src/services/app-sync.ts`
- Modify: `web/src/app/(user)/canvas/stores/use-canvas-store.ts`

- [ ] **Step 1: 保持 manifest 数据形态不变**

`syncAppDataToWebdav` 暂时继续导出 `{ projects: CanvasProject[] }`，避免和远端旧数据不兼容。

- [ ] **Step 2: `replaceProjects` 写入分片**

修改 canvas store 的 `replaceProjects`，让远端合并后的项目写回分片存储。

- [ ] **Step 3: 验证**

Manual Expected：

- 本地两个项目同步到 WebDAV。
- 清空本地数据后重新同步，两个项目恢复。
- 媒体文件能按 storageKey 下载恢复。

## 里程碑 8：工作台复用和取消

### Task 8.1：抽通用日志仓储

**Files:**
- Create: `web/src/services/workbench/log-store.ts`
- Modify: `web/src/app/(user)/image/page.tsx`
- Modify: `web/src/app/(user)/video/page.tsx`
- Modify: `web/src/services/app-sync.ts`

- [ ] **Step 1: 创建 log store 工厂**

```ts
import localforage from "localforage";

export function createWorkbenchLogStore<T extends { id: string; createdAt?: number | string }>(storeName: string) {
    const store = localforage.createInstance({ name: "infinite-canvas", storeName });
    return {
        async list() {
            const logs: T[] = [];
            await store.iterate<T, void>((value) => {
                if (value && typeof value === "object") logs.push(value);
            });
            return logs.sort((a, b) => Number(new Date(b.createdAt || 0)) - Number(new Date(a.createdAt || 0)));
        },
        async save(log: T) {
            await store.setItem(log.id, log);
        },
        async remove(id: string) {
            await store.removeItem(id);
        },
        async replace(logs: T[]) {
            await store.clear();
            await Promise.all(logs.map((log) => store.setItem(log.id, log)));
        },
    };
}
```

- [ ] **Step 2: 替换页面内部 localforage 访问**

图片和视频页面只通过 `createWorkbenchLogStore` 读取、保存、删除、替换日志。

- [ ] **Step 3: 替换 app-sync 中重复日志读取**

`app-sync.ts` 的 `readStoredLogs` 和 `replaceStoredLogs` 可改为复用该工厂，保证同步逻辑和页面逻辑一致。

- [ ] **Step 4: 验证**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run typecheck
```

Manual Expected：图片和视频历史记录能正常新增、预览、删除、同步。

### Task 8.2：图片工作台添加取消

**Files:**
- Modify: `web/src/app/(user)/image/page.tsx`

- [ ] **Step 1: 增加 controller ref**

```ts
const generationControllerRef = useRef<AbortController | null>(null);
```

- [ ] **Step 2: 生成开始时创建 controller**

```ts
const controller = new AbortController();
generationControllerRef.current = controller;
```

- [ ] **Step 3: 传入请求**

将：

```ts
requestEdit(snapshot.config, snapshot.text, snapshot.references)
requestGeneration(snapshot.config, snapshot.text)
```

改为：

```ts
requestEdit(snapshot.config, snapshot.text, snapshot.references, undefined, { signal: controller.signal })
requestGeneration(snapshot.config, snapshot.text, { signal: controller.signal })
```

- [ ] **Step 4: 增加取消按钮处理**

```ts
const cancelGeneration = () => {
    generationControllerRef.current?.abort();
    generationControllerRef.current = null;
    setRunning(false);
    setStartedAt(0);
};
```

- [ ] **Step 5: 用户取消不保存失败日志**

在 catch 中判断错误消息为 `请求已取消` 或 `AbortError` 时，只更新 UI，不写失败日志。

- [ ] **Step 6: 验证**

Manual Expected：开始生图后点击取消，pending 状态停止，不生成失败日志，不影响下一次生成。

### Task 8.3：视频工作台添加轮询取消

**Files:**
- Modify: `web/src/app/(user)/video/page.tsx`

- [ ] **Step 1: 为 active log 保存 controller**

```ts
const activeControllersRef = useRef(new Map<string, AbortController>());
```

- [ ] **Step 2: poll 时传入 signal**

在 `pollGenerationLog` 中创建 controller，并调用：

```ts
const state = await pollVideoGenerationTask(configOverride || taskConfig, log.task, { signal: controller.signal });
```

- [ ] **Step 3: delay 支持取消**

页面内部 `delay` 改为接受 signal，或复用 `services/api/video.ts` 中已有的 signal delay 语义。

- [ ] **Step 4: 添加取消当前任务入口**

```ts
const cancelVideoPolling = (logId: string) => {
    activeControllersRef.current.get(logId)?.abort();
    activeControllersRef.current.delete(logId);
};
```

- [ ] **Step 5: 验证**

Manual Expected：创建视频任务后取消轮询，日志保留“生成中”或标记“已暂停”，刷新页面后可恢复轮询。

## 里程碑 9：Canvas Agent 加固

### Task 9.1：隐藏完整 token 日志

**Files:**
- Create: `canvas-agent/src/security.ts`
- Modify: `canvas-agent/src/http-server.ts`

- [ ] **Step 1: 创建 token 摘要函数**

```ts
export function tokenSummary(token: string) {
    if (token.length <= 8) return "****";
    return `${token.slice(0, 4)}...${token.slice(-4)}`;
}
```

- [ ] **Step 2: 替换启动日志**

将：

```ts
console.log(`Connect token: ${config.token}`);
```

改为：

```ts
console.log(`Connect token: ${tokenSummary(config.token)}`);
console.log("Open /config locally or inspect ~/.infinite-canvas/canvas-agent.json to retrieve the full token.");
```

- [ ] **Step 3: 验证**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\canvas-agent
bun install --frozen-lockfile
bun run build
```

Expected：build 通过，启动日志不出现完整 token。

### Task 9.2：限制附件大小

**Files:**
- Modify: `canvas-agent/src/http-server.ts`
- Modify: `canvas-agent/src/types.ts`

- [ ] **Step 1: 定义限制**

```ts
const MAX_ATTACHMENT_BYTES = 10 * 1024 * 1024;
const MAX_ATTACHMENTS_BYTES = 25 * 1024 * 1024;
```

- [ ] **Step 2: 校验 attachments**

在 `/agent/codex/turn` 读取 attachments 后，计算 base64 数据大小，超过限制返回 413：

```ts
const totalBytes = attachments.reduce((sum, item) => sum + Math.ceil((item.dataUrl || "").length * 0.75), 0);
if (totalBytes > MAX_ATTACHMENTS_BYTES) {
    return void res.status(413).json({ ok: false, error: "attachments too large" });
}
```

- [ ] **Step 3: 验证**

Manual Expected：上传超大附件时接口返回 413，正常小图片仍能发送给 Agent。

## 里程碑 10：提示词 API 整理

### Task 10.1：调整 coverUrl 类型

**Files:**
- Modify: `web/src/app/api/prompts/route.ts`
- Modify: `web/src/services/api/prompts.ts`
- Modify: `web/src/components/prompts/prompt-card.tsx`
- Modify: `web/src/components/prompts/prompt-detail-dialog.tsx`

- [ ] **Step 1: 类型改为可选**

将 Prompt 类型中的：

```ts
coverUrl: string;
```

改为：

```ts
coverUrl?: string;
```

- [ ] **Step 2: API 返回时清理空字符串**

在 prompt 构建结果处，将空字符串转为 undefined：

```ts
coverUrl: item.coverUrl?.trim() || undefined
```

- [ ] **Step 3: UI 只在存在 coverUrl 时渲染图片**

卡片和详情弹窗保持已有空 src 修复，但类型层面不再要求空字符串。

- [ ] **Step 4: 验证**

Run:

```powershell
cd D:\BloomReel\infinite-canvas\web
bun run typecheck
```

Manual Expected：提示词列表无空图片请求，详情弹窗正常。

### Task 10.2：增加提示词源失败可观测性

**Files:**
- Modify: `web/src/app/api/prompts/route.ts`

- [ ] **Step 1: 捕获错误时记录源**

将 `catch { return []; }` 改为：

```ts
} catch (error) {
    console.warn(`[prompts] failed to load ${category.category}`, error instanceof Error ? error.message : error);
    return [];
}
```

- [ ] **Step 2: 响应中增加可选 diagnostics**

仅在请求带 `debug=1` 时返回失败源统计，普通用户响应不变。

- [ ] **Step 3: 验证**

Manual Expected：断网或修改源地址后，服务端日志能看到失败源；普通 `/api/prompts` 响应结构保持兼容。

## 里程碑 11：CI 和文档

### Task 11.1：新增 GitHub Actions 质量门禁

**Files:**
- Create: `.github/workflows/quality.yml`

- [ ] **Step 1: 创建 workflow**

```yaml
name: Quality

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - run: bun run typecheck
      - run: bun run test:run

  canvas-agent:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: canvas-agent
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - run: bun run build
```

- [ ] **Step 2: 验证 YAML 格式**

Run:

```powershell
cd D:\BloomReel\infinite-canvas
git diff --check .github/workflows/quality.yml
```

Expected：无 whitespace errors。

### Task 11.2：更新安全文档

**Files:**
- Modify: `SECURITY.md`
- Modify: `README.md`

- [ ] **Step 1: SECURITY 增加 WebDAV proxy 边界**

写入以下要点：

- direct 模式由浏览器直接访问 WebDAV，受浏览器 CORS 限制。
- nextjs proxy 模式由服务端转发 WebDAV，请部署者仅在可信环境开启。
- proxy 默认拒绝私网和 metadata 地址。
- WebDAV 密码和 API Key 可在设置中清理。

- [ ] **Step 2: README 增加质量命令**

写入：

```powershell
cd web
bun run typecheck
bun run test:run

cd ..\canvas-agent
bun run build
```

- [ ] **Step 3: 验证**

Run:

```powershell
cd D:\BloomReel\infinite-canvas
git diff --check SECURITY.md README.md
```

Expected：无 whitespace errors。

## 交付顺序建议

1. Task 0.1 到 0.3：建立脚本和测试工具。
2. Task 1.1 到 1.2：清掉类型错误并恢复构建门禁。
3. Task 2.1 到 2.3：补核心纯函数测试。
4. Task 3.1 到 3.2：修 WebDAV SSRF。
5. Task 4.1 到 4.2：抽 API 请求基础。
6. Task 6.1 到 6.3：开始画布低风险拆分。
7. Task 8.1 到 8.3：工作台复用和取消。
8. Task 7.1 到 7.2：项目存储分片。
9. Task 9.1 到 9.2：Canvas Agent 加固。
10. Task 10.1 到 10.2：提示词 API 整理。
11. Task 11.1 到 11.2：CI 和文档收尾。

## 完成定义

- `web`：`bun run typecheck`、`bun run test:run`、`bun run build` 通过。
- `canvas-agent`：安装依赖后 `bun run build` 通过。
- `docs`：安装依赖后 `bun run types:check` 通过。
- WebDAV proxy 安全测试通过。
- 画布手动回归清单通过。
- 图片和视频工作台取消行为一致。
- 项目存储迁移后旧项目可见，媒体引用可恢复。
