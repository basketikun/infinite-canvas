# Optional hosted Pi Agent. Leave all three unset to keep the existing local-only experience.
# For the nginx container, pass the same public values at runtime without the VITE_ prefix.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key
VITE_AGENT_API_URL=http://localhost:4100
