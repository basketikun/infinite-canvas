# 待测试

- 通过局域网 IP 访问前台提示词、素材库、我的素材和后台相关页面时，复制文本应能正常写入剪贴板。
- 管理后台「用户管理」CRUD：新增/编辑/删除用户，角色和生图额度修改后立即生效；调整额度后流水里出现 `admin_adjust` 记录。
- 管理后台「模型配置」CRUD：新增、编辑、删除、启用切换、连通测试均正常；API Key 编辑留空保持原值，删除已启用配置应被拒绝。
- 普通用户额度扣减：生图成功后顶部「积分」实时减少，余额为 0 时再次调用提示「额度不足，请联系管理员」；个人中心流水出现 `consume` 记录。
- 管理员账号生图：顶部显示「∞」，不消耗额度，不写流水。
- 文本问答限流：1 分钟内连续触发 6 次，第 6 次应被拒绝并提示等待秒数；管理员不受限。
- 注册流程：开放注册后，新账号默认 4 点额度；个人中心首条流水为 `signup_bonus +4`；密码不一致、用户名重复时提示清晰。
- 前台「管理后台」入口：仅管理员账号在用户菜单中可见；普通用户直接访问 `/admin` 会被重定向。
- 反代路径：前端不再持有 API Key，所有生图/聊天请求经 `/api/v1/*` 转发到上游。
- 未配置启用模型时，普通生图调用应返回「未配置启用的模型，请联系管理员」。
- 我的画布上云：新建/重命名/删除画布、画布内节点编辑都会自动保存到后端；不同账号在同一浏览器互不可见；换设备登录能看到画布列表与文字结构（图片节点因图床未上云仍可能加载失败）。
- 我的素材上云：新增/编辑/删除走 `/api/assets/me`；不同账号互不可见。
- 生图工作台历史上云：生成完成后顶部历史出现新记录；刷新页面后历史仍在；当前会话能看到缩略图，跨刷新仅显示参数与状态。
- 默认生成尺寸：所有用户首次进入工作台或画布配置节点，默认 size = `auto`、count = `1`。
- 个人中心 `/profile`：顶部卡片显示当前积分、用户名、角色、注册时间；三张统计卡显示累计消耗 / 获赠 / 生图次数；流水表分页正常，颜色区分变动方向。
- 顶栏积分 Tag：普通用户显示蓝色「N 积分」，管理员显示金色「∞」；点击跳转 `/profile`。
- 浏览器本地图片缓存隔离：账号 A 上传的图片，账号 B 登录后看不到（不出现旧 A 的图片缩略图）。
- 管理后台「模型配置」新增配置时：弹窗默认填充图像模型 `gpt-image-2`、文本模型 `gpt-5.4`；填好 Base URL + API Key 后点「获取模型列表」可拉取上游模型，图像/文本模型输入框下拉显示候选；编辑配置时 API Key 留空，也能用库里原 Key 探测；Base URL 或 Key 未填时点击按钮会给出对应提示。
- 长耗时生图：画布或工作台触发 `/api/v1/images/generations`、`/api/v1/images/edits`，即使上游耗时 >30 秒也能成功返回，不再出现 `Internal Server Error / socket hang up`。
- 生图工作台深链：浏览器登录后访问 `/image`，点击左侧任意一条记录，URL 应切到 `/image/{id}`，右侧自动展开；浏览器刷新或新开标签直接访问 `/image/{id}` 也能进入对应记录详情；点击"新建"按钮 URL 回到 `/image`。
- 图片缓存丢失提示：换一个浏览器或清掉 IndexedDB 后登录账号，再打开历史里的旧生成记录，原图应显示琥珀色「图片缓存丢失」卡片，而不是红色"生成失败"卡片；提示词、尺寸、数量等参数仍能正常回填。
- 图片上云端到端：在 A 浏览器生成一张图并保存为素材或加进画布，在 B 浏览器（甚至换设备）登录同一账号打开同一条记录或同一画布，图片应能正常展示（不出现缓存丢失卡）。删除图片节点或素材后，对应的 `images` 行也应被清掉（前端 deleteStoredImages 会发 DELETE）。
- 跨设备生图偏好：在 A 浏览器把生图次数改成 3、尺寸改成 3:2，等约 1 秒后在 B 浏览器登录同一账号刷新页面，生图工作台默认值应同步显示 3 与 3:2。新注册账号默认仍是 1 与 auto。
- 画布详情页积分 Tag：进入 `/canvas/[id]` 后右上角用户头像左侧出现积分 Tag；普通用户为蓝色「N 积分」、管理员为金色「∞」；点击跳转 `/profile`；生图成功后数值实时减少，生图失败（额度不足/上游报错/未返回图片）不扣减。
- 生图工作台切换记录：在 `/image` 左侧"生成记录"中点选任意一条历史记录，右侧"生图工作台"的提示词、生成次数、尺寸、质量应回填为该记录的参数（参考图不会恢复）；"生成结果"区：成功张数显示图片，失败张数显示红色"生成失败"占位卡片；全失败的记录右侧不再是空白。
- 图片落盘：上传或生成图片后，服务器 `IMAGE_DIR`（默认 `data/uploads/{userId}/`）下出现对应 `.png` / `.jpg` 文件；数据库 `images` 表只看到 path 字段、不再有 BLOB。删除画布节点 / 素材 / 生成记录后，对应文件被同步删除。
- 图片直链：浏览器开发者工具 Network 面板查看，`<img>` 标签直接发 `GET /api/images/{id}`，**不带** Authorization 头也能 200；命中 `Cache-Control: public, max-age=86400, immutable`；同图刷新页面走 304 / disk cache。
- 大图上传：在 `/image` 工作台上传 ≥5MB 的参考图（PNG/JPG 都试），不再返回 504 / 413；上传完成后历史记录里可见，能保存到画布。
- `/api/images` 直转后端：浏览器 Network 面板看 POST `/api/images` 上传一张 10MB+ 图片，请求**应在合理时间（几秒内）内返回**，不出现长时间 pending；同样大小的图片在生图成功后自动落库时也不卡。GET `/api/images/:id` 仍然能直接被 `<img>` 拉到（200 + 缓存头）。
- 旧 BLOB 数据：升级前已经写过图的 sqlite，本次升级后这些 BLOB 行会变成"path 为空 + 文件不在磁盘"，访问 `/api/images/{id}` 返回 `图片文件丢失`；前端对应位置显示「图片缓存丢失」卡片。提示词等元信息仍可见，不影响其他流程。
- 生图工作台重试持久化：在 `/image/{id}` 页面上传参考图后对某个失败 slot 点「重试」，成功后刷新页面（或者新开标签直接进 `/image/{id}`），左侧参考图列表和右侧成功的图片都应原样恢复；多次重试不同 slot，记录里的 successCount/failCount 也同步更新。
- 跨设备生图记录参考图：在 A 浏览器生成（带参考图，可能首次失败再重试），在 B 浏览器登录同一账号打开同条记录，参考图能正常显示。
- 生图记录越权防护：用 curl 带账号 A 的 token 调 `POST /api/generations` 传账号 B 的某条 generation id，应返回「权限不足」；传一个完全不存在的 id 应返回「生成记录不存在」。
- 管理后台「生图记录」页：以管理员登录进 `/admin/generations`，能看到所有用户的调用，关键词输入用户名 / 提示词均能搜到；选「全部成功 / 部分成功 / 全部失败」筛选生效；点击行打开详情弹窗，提示词、参考图、生成结果缩略图全部正常显示；普通用户访问 `/admin/generations` 应被重定向。
- 管理后台「积分流水」页：`/admin/credit-logs` 列出所有用户流水，能看到本次会话内的注册赠送、生图扣减、管理员调整记录；管理员调整一行的「操作员」列显示操作管理员用户名；类型筛选与关键词搜索生效。
- 画布助手图片上传：进入任意画布右侧助手面板，点击输入框左下「上传图片」按钮可选择本地文件；把图片直接拖拽到输入框区域时出现蓝色高亮"松开以添加图片"提示，松手后参考图 chip 出现，效果跟粘贴一致；拖入非图片类型应被忽略；一次拖多张应全部加入。
- 切换账号 /canvas 不残留：账号 A 登录进 `/canvas`，看到自己的画布列表，登出后用账号 B 登录回到 `/canvas`，**不应**先短暂闪现 A 的画布——应直接进入 loading 状态，等 B 的列表拉回再展示；不需要手动刷新。
- 生图工作台两阶段入库：在 `/image` 点「开始生成」的**瞬间**，左侧记录列表出现一条「进行中」金色 tag 新记录，URL 切到 `/image/{id}`；task 全部跑完后这条记录的 tag 变成"成功 / 失败"计数。中途刷新页面也能在历史里看到这条 running 记录，进入它右侧每个 slot 显示「生成被中断，请点击重试」（与普通"生成失败"区分），点重试可继续。`/admin/generations` 列表「状态」筛选选「正在生成」也能筛出 running 记录，颜色金色。
- 画布图片放大查看：
  - 进入任意画布，悬浮到图片节点上 → hover 工具栏出现「放大查看」按钮（位于「下载图片」左侧），点击弹出 antd 预览浮层，可缩放/旋转/键盘 ←→ 切换；
  - 助手面板里助手发回的图、参考图 chip 缩略图，点击图片本身即可放大；点击 chip 上「×」删除仍正常；
  - 配置节点上下游输入图缩略图，点击放大；按住缩略图拖动不会预览，节点拖拽仍可触发。
- 413 友好提示：把一张 ≥50MB 的图片（或几张加起来超过 50MB 的参考图）拖入画布助手 / 工作台上传 / 图生图，提示文案应是「图片素材太大了（最多 50MB），请压缩或裁剪后再上传」或「请求体过大…」，**不会**出现 "图片上传失败 HTTP 413""请求失败：413" 这类技术信息；50MB 以内的图正常上传。
- 更新日志页：点击顶栏右侧版本号（已登录态在用户头像左侧、未登录态在登录按钮左侧），跳转 `/changelog`，能看到 Timeline 时间线，每个版本下面是 `[新增/修复/调整]` 标签彩色 Tag + 条目文字；最顶上"Unreleased"显示灰色节点 + "开发中" Tag；Docker 部署后页面同样能展示（因为 Dockerfile 把根目录 CHANGELOG.md COPY 到了容器 `/app/CHANGELOG.md`）。
- 上传图片 loading：在 `/image` 工作台点击「上传参考图」按钮、从剪切板粘贴、把图拖到画布、点击画布节点的"替换图片"或者"裁剪并重生成"、`/assets` 新增素材时上传图、`/asset-library` 把公开图加入我的素材，**等待期间**右上角都应出现 antd 蓝色 spinner toast「正在上传XX…」；上传完成 toast 自动消失（成功不再弹 success 干扰）；上传失败 toast 变红字显示中文错误（含 413 友好提示）。
- 管理后台「生图记录」详情完整审计：以管理员登录访问 `/admin/generations`，点开任意一条记录详情，应能看到：① 完整提示词（不截断）；② 参考图 + 生成结果缩略图（已有）；③【新】若有失败 slot，「错误信息」区按行列出 error.message；④【新】「请求参数」区以 JSON 显示 `{mode, n, size, quality, referenceCount}`；⑤【新】若上游成功调用过，「上游响应」区以美化 JSON 显示 OpenAI 兼容接口的 raw response（`b64_json` 已替换为 `<N bytes redacted>` 不会让弹窗膨胀）。整批失败的记录也能看到这些字段（errors 长度 = count），方便定位上游错误。
- 上游 504/502/503 友好提示：当 ChatGPT/chatgpt2api 网关层返回 nginx HTML 错误页（典型如 504 Gateway Time-out）时，前端生成结果卡片显示的错误**不再**是一整段 `<html>...<title>504 Gateway Time-out</title>...`，而是中文友好提示「上游服务响应超时（504），请稍后再试」。`/admin/ai-configs`「测试」按钮在上游 504/502/503 时同样显示中文。
- 图生图传 storageKey：在 `/image` 工作台拖入素材库 / 历史结果作为参考图（这些图都已经在服务器图床），点开始生成；浏览器 DevTools Network 查看 `POST /api/v1/images/edits`：Content-Type 应是 `application/json`，Payload 看到 `references: ["img-xxx", ...]` 而**不是** multipart；请求体只有几百字节，正常返回图片。换一个还没上传到服务器的瞬时图（如画布刚截屏），同一接口应自动回落到 multipart，仍能正常工作。
- 生图全程不卡死：进 `/image`（不带 id），点「开始生成」，**整个过程**应观察到：① 左侧立刻出现金色「进行中」记录卡；② URL 切到 `/image/{gen-xxx}`；③ 右侧 slot 保持「正在生成」pending 状态（loading 占位）**而不是「生成被中断」**；④ task 跑完后 slot 变成图片（成功）或红色失败卡（真失败）。如果生成实际成功（服务器 `/api/v1/images/generations` 200、`/api/images` 200）但 UI 显示"被中断"，说明这条 race 修复失效，需要重新审视 layout 是否生效。
- 切换记录不影响进行中的生成：A 生成跑到一半，从左侧记录列表点开另一条历史记录，左侧表单 / 右侧 results 切到历史记录；A 跑完后**不**自动覆盖当前查看的历史。重新点回 A 那条 running 的记录，能看到 task 实际完成的图片（不是「被中断」）。
- 图生图 references 越权防护：以账号 A 登录拿一张账号 B 的 image id（让 admin 在 `/admin/generations` 偷一个 storageKey），构造 JSON 调 `/api/v1/images/edits` 传 `references: ["{B 的 id}"]`，应返回 `参考图无权访问`；传一个完全不存在的 id 应返回 `参考图不存在`；同一请求里塞 9 张及以上 references 应返回 `参考图数量超过上限（最多 8 张）`。
