# Workspace 测试插件

用于验证 Infinite Canvas Phase 1 的通用全屏 Plugin Workspace：

- 节点双击或按钮可以打开 Workspace；
- Workspace 内的“返回画布”和 Escape 可以关闭；
- 关闭后 Canvas 节点、选区和 viewport 应保持不变。

该插件不包含 Blockout 或 Director 逻辑。
